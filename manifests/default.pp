package {
	"build-essential": ensure => latest;
	"mingw-w64-i686-dev": ensure => latest;
	"binutils-mingw-w64-i686": ensure => latest;
	"gcc-mingw-w64-base": ensure => latest;
	"gcc-mingw-w64-i686": ensure => latest;
	"g++-mingw-w64-i686": ensure => latest;
	"pkg-config": ensure => latest;
	"dos2unix": ensure => latest;
}